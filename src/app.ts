var clientSocket = io();




window.onload = () => {

    var game = new HideAndSeek.Game();
    
};

function equivalent(a, b)
{
    if (a == b)
    {
        return true;
    } else {
        return false;
    }
}
///// <reference path="../../typings/main.d.ts" />
 
describe("MainMenuState", () => {
 
    describe("The main menu state", () => {
 
        it("Holds the main menu state information", () => {
 
            // Arrange
            let mms = new HideAndSeek.MainMenuState();
 
            // Act
            let result = mms.create();
 
            // Assert
            //expect(result).
        });
    });
});